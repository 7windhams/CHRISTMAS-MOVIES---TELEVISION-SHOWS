<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Additional festive styles for homepage */
        .stats {
            background: linear-gradient(135deg, var(--christmas-gold), var(--christmas-gold-dark));
            padding: 25px;
            border-radius: 20px;
            box-shadow: var(--card-shadow);
            margin-bottom: 30px;
            text-align: center;
            color: var(--snow-white);
            border: 3px solid var(--christmas-red);
            position: relative;
            overflow: hidden;
        }
        .stats::before {
            content: '🎅🎄🎁';
            position: absolute;
            top: 10px;
            right: 15px;
            font-size: 20px;
            opacity: 0.7;
        }
        .stats h2 {
            color: var(--snow-white);
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            font-size: 28px;
            margin-bottom: 15px;
        }
        .total-count {
            font-size: 48px;
            font-weight: bold;
            color: var(--christmas-red);
            text-shadow: 2px 2px 4px rgba(255,255,255,0.8);
            animation: countPulse 2s ease-in-out infinite;
        }
        @keyframes countPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="christmas-theme"><%= title %></h1>
            <p style="color: #FFD700; font-size: 24px; margin: 15px 0; font-weight: 700; text-shadow: 3px 3px 6px rgba(0,0,0,0.9); background: rgba(220,20,60,0.2); padding: 12px 20px; border-radius: 12px; border: 2px solid #FFD700; letter-spacing: 1px;">
                Your comprehensive database for holiday movies and TV shows
            </p>
        </div>

        <div class="navigation">
            <a href="/" class="nav-link">Home</a>
            <a href="/programs" class="nav-link">All Programs</a>
            <a href="/actors" class="nav-link">Actors</a>
            <a href="/add-actor" class="nav-link">Add Actor</a>
            <a href="/add-program" class="nav-link">Add Program</a>
        </div>

        <div class="stats">
            <h2>Collection Overview</h2>
            <p style="font-size: 18px; margin-bottom: 20px;">Browse our curated collection of Christmas entertainment</p>
            <div class="total-count"><%= totalPrograms %></div>
            <p style="font-size: 16px; margin-top: 15px;">Total Programs Available</p>
        </div>
        <div class="programs-preview">
            <h2 style="text-align: center; color: var(--christmas-red); font-size: 28px; margin: 40px 0 30px 0; font-weight: 600;">
                Featured Programs
            </h2>
            <div class="grid grid-3">
                <% programs.forEach(program => { %>
                    <div class="program-card">
                        <div class="program-title"><%= program.title %></div>
                        <div style="display: flex; flex-wrap: wrap; gap: 8px; margin: 15px 0;">
                            <span class="program-meta"><%= program.yr_released %></span>
                            <span class="program-meta"><%= program.format %></span>
                            <span class="program-meta"><%= program.program_rating %></span>
                            <% if (program.rating) { %>
                                <span class="program-meta">★ <%= program.rating %>/10</span>
                            <% } %>
                        </div>
                        <div class="program-description" style="margin: 15px 0; padding: 12px; background: #f8f9fa; border-radius: 4px; border-left: 3px solid var(--christmas-red); font-size: 14px; line-height: 1.5;">
                            <%= program.description && program.description.length > 120 ? program.description.substring(0, 120) + '...' : program.description || 'No description available' %>
                        </div>
                        <% if (program.producer_name) { %>
                            <div style="margin: 8px 0; font-size: 14px; color: #666;">
                                Producer: <strong><%= program.producer_name %></strong>
                            </div>
                        <% } %>
                    </div>
                <% }); %>
            </div>
            <div style="text-align: center; margin: 30px 0;">
                <a href="/programs" class="btn btn-primary">
                    View All Programs
                </a>
            </div>
        </div>


        <div class="api-section card">
            <h2 style="color: var(--christmas-red); text-align: center; font-size: 24px; font-weight: 600;">
                API Documentation
            </h2>
            
            <div style="background: #f8f9fa; border-left: 4px solid var(--christmas-green); border-radius: 4px; padding: 15px; margin: 15px 0;">
                <h3 style="color: var(--christmas-green-dark); margin-top: 0; font-size: 16px;">GET /api/programs</h3>
                <p style="margin: 8px 0; font-size: 14px;">Retrieve all Christmas programs with details</p>
                <code style="background: #e9ecef; color: #495057; padding: 4px 8px; border-radius: 3px; font-size: 13px;">http://localhost:3000/api/programs</code>
            </div>

            <div style="background: #fff5f5; border-left: 4px solid var(--christmas-red); border-radius: 4px; padding: 15px; margin: 15px 0;">
                <h3 style="color: var(--christmas-red-dark); margin-top: 0; font-size: 16px;">GET /api/movies</h3>
                <p style="margin: 8px 0; font-size: 14px;">Get only Christmas movies from the database</p>
                <code style="background: #e9ecef; color: #495057; padding: 4px 8px; border-radius: 3px; font-size: 13px;">http://localhost:3000/api/movies</code>
            </div>

            <div style="background: #f0f4ff; border-left: 4px solid var(--winter-blue); border-radius: 4px; padding: 15px; margin: 15px 0;">
                <h3 style="color: var(--winter-blue); margin-top: 0; font-size: 16px;">GET /api/search</h3>
                <p style="margin: 8px 0; font-size: 14px;">Search programs by title or description</p>
                <code style="background: #e9ecef; color: #495057; padding: 4px 8px; border-radius: 3px; font-size: 13px;">http://localhost:3000/api/search?q=home</code>
            </div>
        </div>

        <div style="text-align: center; margin: 30px 0; padding: 20px; background: white; border-radius: 6px; box-shadow: 0 2px 6px rgba(0,0,0,0.08);">
            <p style="font-size: 16px; color: #666; margin: 0;">
                Explore our collection of holiday entertainment
            </p>
        </div>
    </div>
</div>
</body>
</html>